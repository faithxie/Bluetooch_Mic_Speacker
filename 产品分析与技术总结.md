# 蓝牙音频路由软件 — 产品分析与技术总结

> 版本：v1.0  
> 日期：2026-09-18  
> 项目代号：BTSPK (Bluetooth Speaker)

---

## 一、产品概述

### 1.1 产品定位

一款 Windows 本地音频路由工具，将蓝牙耳机的麦克风声音**实时**路由到电脑扬声器/HDMI 输出，实现「耳返监听」功能。核心场景是让用户通过蓝牙耳机说话，声音从电脑音箱/外接音响播出，适用于演讲、K歌、直播监听、语音教学等场景。

### 1.2 核心功能

| 功能模块 | 功能描述 | 状态 |
|---------|---------|------|
| 设备管理 | 枚举所有输入/输出音频设备，智能识别蓝牙设备、HDMI、内置扬声器 | ✅ |
| 实时路由 | 麦克风声音 → 实时处理 → 扬声器输出，低延迟 | ✅ |
| 采样率转换 | 自动处理不同采样率设备（如 16kHz 蓝牙 → 48kHz 扬声器） | ✅ |
| 声道转换 | 单声道麦克风 → 立体声扬声器自动扩展 | ✅ |
| 麦克风增益 | 0~500% 数字增益，补偿蓝牙耳机麦克风音量低的问题 | ✅ |
| 输出音量 | 0~200% 数字音量控制 | ✅ |
| 颤音调制抑制 | 频率调制(FM)破坏啸叫共振（核心算法） | ✅ |
| 自适应陷波 | 自动检测啸叫频率并精准消除 | ✅ |
| 噪声门 | 不说话时自动静音，切断回音环路 | ✅ |
| 侧链抑制 | 扬声器有声时自动压低麦克风增益 | ✅ |
| 实时电平表 | 输入/输出音量可视化 | ✅ |
| 延迟/缓冲监控 | 实时显示延迟、缓冲占用、欠载次数 | ✅ |
| 设备可用性检测 | 启动前自动测试设备能否正常打开 | ✅ |
| Host API 优先级 | 优先使用 WASAPI（最可靠），排除 WDM-KS（不支持回调） | ✅ |
| 参数自动容错 | 自动尝试 15 种参数组合确保启动成功 | ✅ |

### 1.3 目标用户

- **演讲者/培训师**：蓝牙耳机当无线麦，声音从会场音响输出
- **K歌爱好者**：实时耳返监听
- **直播主播**：监听自己的声音效果
- **语音教学**：老师用蓝牙耳机上课，声音从教室音响播出
- **特殊需求用户**：需要将麦克风声音实时放大的场景

### 1.4 竞品对比

| 特性 | 本软件 | VoiceMeeter | 系统自带 | 专业效果器 |
|------|--------|-------------|----------|-----------|
| 安装体积 | 轻量（~50MB） | 较大 | 无 | 大 |
| 操作复杂度 | 简单（一键启动） | 复杂 | 无 | 专业级 |
| 啸叫抑制 | 4 级防护 | 基础 | 无 | 专业级 |
| 延迟 | ~20ms（处理）+ 蓝牙延迟 | 较高 | 低 | 中等 |
| 免费 | ✅ | 基础版免费 | ✅ | 付费 |
| 针对性 | 蓝牙→扬声器专用 | 通用音频路由 | 无 | 通用效果处理 |

---

## 二、系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     UI 层 (Tkinter)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ │
│  │ 设备选择  │  │ 音量增益  │  │ 防啸叫   │  │ 电平表 │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘ │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   服务层 (Services)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐ │
│  │ DeviceManager│  │ AudioRouter  │  │ FeedbackSupp. │ │
│  │ 设备管理      │  │ 核心路由引擎  │  │ 啸叫抑制      │ │
│  └──────────────┘  └──────┬───────┘  └───────┬───────┘ │
│                           │                  │         │
│                     输入回调            输出处理链     │
└───────────────────────────┬──────────────────┬─────────┘
                            │                  │
┌───────────────────────────▼──────────────────▼─────────┐
│                PortAudio (sounddevice)                  │
│              WASAPI / DirectSound / MME                 │
└─────────────────────────────────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              ▼                           ▼
      蓝牙耳机麦克风                扬声器/HDMI
```

### 2.2 目录结构

```
Bluetooth_Mic_Speaker/
├── main.py                       # 程序入口
├── start.bat                     # 启动脚本
├── requirements.txt              # 依赖清单
├── README.md / LICENSE
├── audio_router/
│   ├── __init__.py
│   ├── models/
│   │   ├── __init__.py
│   │   └── audio_device.py       # 音频设备信息模型 + 类型识别（蓝牙/HDMI/虚拟）
│   ├── services/
│   │   ├── __init__.py
│   │   ├── device_manager.py     # 设备管理服务
│   │   ├── audio_router.py       # 核心路由引擎
│   │   ├── howling_suppressor_v2.py        # 啸叫抑制算法 v2（真移频 + 多判据）
│   │   └── feedback_suppressor_deprecated.py # 【已废弃】旧算法，仅作历史记录
│   └── ui/
│       ├── __init__.py
│       └── main_window.py        # Tkinter 主界面（三段式布局）
├── diagnostics/                  # 诊断与验证脚本
├── aec_experiment/               # AEC 可行性验证（探索性，结论：不采用）
├── 产品分析与技术总结.md
└── 啸叫问题诊断与解决方案报告.md
```

**总代码量**：约 3,000 行 Python 代码

### 2.3 技术栈

| 类别 | 技术选型 | 说明 |
|------|---------|------|
| 开发语言 | Python 3.10 | 开发效率高，音频库丰富 |
| 音频引擎 | PortAudio (sounddevice) | 跨平台音频 I/O，支持 WASAPI |
| 数值计算 | NumPy 2.2 | 高效数组运算 |
| 信号处理 | SciPy 1.15 | 重采样、信号处理工具 |
| GUI 框架 | Tkinter (ttk) | Python 自带，无需额外依赖，启动快 |
| 虚拟环境 | venv | 依赖隔离，避免污染系统 Python |

---

## 三、核心技术点详解

### 3.1 实时音频路由架构

#### 双流架构（输入流 + 输出流独立）

不同于简单的 `Stream`（同时输入输出），本项目使用独立的 `InputStream` 和 `OutputStream`：

```
输入流回调（麦克风数据到达）
    ↓
应用输入增益 → 噪声门 → 侧链抑制 → 声道转换 → 重采样 → 输出音量
    ↓
写入环形缓冲区
    ↓
输出流回调（扬声器需要数据）
    ↓
从环形缓冲区读取 → 颤音调制 → 自适应陷波 → 输出
```

**为什么用双流而不是单一流？**
1. **采样率不同**：蓝牙麦克风通常 16kHz，扬声器 48kHz，单一流不支持不同采样率
2. **独立控制**：输入输出可以独立启停、独立调整参数
3. **缓冲解耦**：输入输出抖动互不影响，更稳定

#### 样本级环形缓冲区

核心数据结构，用于解耦输入和输出：

- **设计**：单线程写入 + 单线程读取，无锁安全（实际加锁保证多声道一致性）
- **大小**：约 500ms 音频数据，防止溢出
- **预热**：启动时填充 100ms 静音，避免蓝牙设备启动慢导致的欠载
- **自动重采样**：输入数据写入前重采样到输出采样率

### 3.2 设备管理策略

#### Host API 优先级排序

Windows 上 PortAudio 提供多个音频 API，可靠性差异大：

| API | 优先级 | 说明 |
|-----|--------|------|
| WASAPI | ⭐⭐⭐ 最高 | 最可靠，延迟低，支持回调模式 |
| DirectSound | ⭐⭐ 中等 | 兼容性好，延迟中等 |
| MME | ⭐ 最低 | 最旧的 API，延迟高，但兼容性最好 |
| WDM-KS | ❌ 排除 | 不支持回调模式（只支持阻塞读写） |
| ASIO | ❌ 排除 | 专业声卡用，普通设备不支持 |

#### 设备可用性预检测

枚举设备后，对每个设备**实际尝试打开流**来验证可用性，避免选中看似存在但无法使用的设备。

#### 智能设备识别

- **蓝牙输入**：关键词匹配（bluetooth、蓝牙、hands-free、bthhfenum、耳机、各品牌名如 dubuds/airpods 等）
- **蓝牙输出排除**：默认输出不选蓝牙耳机，避免声音又回到耳机形成回环
- **HDMI / DisplayPort 输出**：`AudioDeviceInfo.is_display_audio` 关键词表匹配。**注意**：Windows 上这类设备名通常**不含 "hdmi"**，而是以显卡厂商的音频驱动名出现，例如「LED Proj (英特尔(R) 显示器音频)」（Intel Display Audio）。关键词表覆盖 Intel / NVIDIA / AMD 的显示器音频、`displayport`、`projector`、`tv`、`monitor` 等
- **虚拟声卡排除**：`_is_virtual_device()` 排除 VB-Audio / CABLE / 声音映射器 / 主声音驱动等，避免误选
- **内置扬声器**：名称包含"扬声器"且非蓝牙、非虚拟

### 3.3 四级防啸叫算法

> **⚠️ 本节已于 2026-09-18 重写**。初版实现存在三个层面的问题并已修复，
> 详细诊断过程与实测数据见《啸叫问题诊断与解决方案报告.md》。以下描述的是**当前实际实现**。

啸叫（声反馈）的本质是：**扬声器声音 → 麦克风拾取 → 放大 → 扬声器输出** 的正反馈循环，在特定频率形成共振并指数增长。

针对这个问题，实现了 4 层防护，从根源到辅助层层递进：

#### 第 1 级：真移频（True Frequency Shifter / SSB）⭐ 核心

**原理**：把整个输出频谱整体平移 +4Hz（如 1000Hz → 1004Hz）。反馈环路中每次循环频率都会再偏移，
啸叫永远无法稳定在某个频率上，因此**从根源破坏反馈共振**。

**实现细节**：
- **FIR Hilbert 变换器**（63 阶，Hamming 窗）+ 逐样本流式处理
- 解析信号构造：`y = x_delayed·cos(2πΔt) − hilbert(x)·sin(2πΔt)`
- 群延迟补偿：`(taps−1)/2 = 31` 样本，同相支路对齐
- 移频载波相位**跨块累积**，保证块间连续
- 移频量默认 4 Hz（可调 2~12 Hz，推荐 3~5 Hz）

**为什么不用「延迟调制」**：初版用正弦调制延迟时间，那实际是**颤音（FM）**——只把能量散布到边带，
主峰仍在原频率，听感是人声发抖、有金属感。实测：1000Hz 输入经「颤音」后主峰仍是 1000Hz；
换成真移频后主峰精确移到 **1004.00 Hz（误差 0.00 Hz）**。

**音质影响**：真移频是线性操作，只平移频谱、不改变各频段能量分布。
实测 500–2000 Hz 能量占比 94.09% → 95.49%，高频伪影 +0.007%，振幅过冲比 1.0027（无过冲）。

#### 第 2 级：多判据自适应陷波（Multi-Criteria Notch）🎯

**原理**：实时分析输出频谱，用**四个判据的逻辑与**判断是否存在啸叫，只在确认后才放陷波器。

**检测判据**（全部满足才判定为啸叫）：

| 判据 | 含义 | 阈值 |
|------|------|------|
| **PNPR** | Peak-to-Neighbouring Power Ratio：峰比相邻 bin 高出多少（啸叫是极窄的非阻尼正弦） | > 9 dB |
| **PHPR** ★ | Peak-to-Harmonic Power Ratio：该频率有无 2×/3× 谐波能量 | > 40 dB |
| **IMSD** | Interframe Magnitude Slope Deviation：幅度随时间偏离线性(dB)增长的程度 | < 1.0（就绪时作否决票） |
| **PTPR** | Peak-to-Threshold Power Ratio：绝对响度是否足够 | > −45 dB |

**PHPR 是决定性判据**：人声的第 N 次谐波必然伴随 2N、3N 次谐波（有谐波族），
而啸叫是孤立单频、没有谐波。这一条是区分「人声」与「啸叫」的关键。

> 学术依据：van Waterschoot & Moonen, Proc. IEEE 2011《Fifty Years of Acoustic Feedback Control》。
> 单判据误报率实测：PTPR 70%、PAPR 63%、PHPR 37%、PNPR 33%、IPMP 54%、IMSD 40%
> —— **任何单一判据都不可用，必须多判据逻辑与。**

**实现细节**：
- 4096 点 FFT（Blackman 窗），分辨率 11.7 Hz（旧版 512 点仅 93.8 Hz）
- 检测范围 200 Hz ~ 8 kHz，抛物线插值提高频率精度
- 双二阶 IIR 陷波器，**Q = 10**（旧版 Q=30 过窄，定位误差会陷空）
- 陷波衰减默认 **10 dB**（旧版 20 dB 过于激进，误判时对人声破坏不可逆）
- 最多同时激活 4 个陷波器
- **系数一阶平滑过渡**（smooth=0.35），**保留 IIR 状态不重置** → 消除旧版每 ~107 ms 一次的咔哒声

**改进效果（实测）**：

| 指标 | 旧版（单判据） | 现版（多判据） |
|------|---------------|---------------|
| 人声误判 | **66 次**（把元音共振峰 F1/F2/F3 当啸叫削掉） | **0 次** ✓ |
| 啸叫检出 | 检测不出 | **180/187 帧** ✓ |
| 啸叫抑制量 | — | **−24 dB** ✓ |

**为什么旧版会「改变说话声音」**：单一判据「峰比邻域高 6 dB」无法区分人声谐波与啸叫，
实测把 730 / 1193 / 2508 Hz（正是元音共振峰）判为啸叫并挖掉 15~20 dB，
听感就是「发闷、像隔着桶说话」。**这是初版音质问题的头号元凶。**

#### 第 3 级：噪声门（Noise Gate）🚪

**原理**：当输入电平低于阈值时，完全静音输出，直接切断反馈环路。不说话时没有信号，自然不会啸叫。

**实现细节**：
- 阈值范围：0.005 ~ 0.1（对应 -46dB ~ -20dB）
- Attack（开门）系数 0.8：快速打开，避免字首被切掉
- Release（关门）系数 0.95：缓慢关闭，避免字尾被截断和咔哒声
- 平滑增益控制，无开关爆破音

#### 第 4 级：侧链抑制（Sidechain Ducking）📉

**原理**：当输出音量超过阈值时，自动降低输入增益。扬声器声音越大，麦克风增益压得越低，打断正反馈循环。

**实现细节**：
- 输出阈值：0.05（约 -26dB）
- 抑制强度：10% ~ 90% 可调
- 输出电平越高，抑制越强（线性映射）

#### 四级协同工作

```
麦克风输入
    ↓
[输入增益] → [噪声门] → [侧链抑制] → [声道转换] → [重采样] → [输出音量]
    ↓
写入环形缓冲区
    ↓
[多判据自适应陷波] → [真移频] → 扬声器输出
```

**处理顺序说明**：陷波器放在移频之前，让检测算法分析的是**未经频率搬移的原始频谱**，
谱结构更干净、检测更准。（旧版顺序相反，移频产生的边带会干扰陷波检测。）

### 3.4 线程模型与 PortAudio 坑点

#### 线程亲和性问题

**重要发现**：PortAudio/WASAPI 的音频流**必须在创建它的同一个线程中启动**。如果在主线程创建流、在子线程调用 `start()`，会报 `WDM-KS error -9999` 或 `Blocking API not supported yet` 等莫名其妙的错误。

**解决方案**：引擎的 `start()` 方法必须在主线程调用。虽然启动操作本身很快（几十毫秒），不会明显卡 UI，但这是必须遵守的约束。

#### 启动容错策略

由于不同设备、不同 API、不同参数组合的兼容性差异很大，采用自动容错启动：

```
尝试 15 种参数组合（3 种 latency × 5 种 blocksize）：
  latency: default → high → low
  blocksize: auto → 1024 → 2048 → 512 → 256

每个组合：
  1. 创建输入流
  2. 创建输出流
  3. 启动输出流
  4. 启动输入流
  全部成功 → 运行
  任何一步失败 → 清理 → 试下一组
```

### 3.5 重采样与声道处理

#### 采样率转换

蓝牙麦克风通常是 16kHz（通话质量），扬声器是 48kHz，采样率差 3 倍。

**实现方案**：
- 使用 `scipy.signal.resample`（傅里叶重采样，音质好）
- 在输入回调中完成，写入缓冲区前就转成输出采样率
- 环形缓冲区按输出采样率组织

#### 声道转换

- 单声道麦克风 → 立体声扬声器：`np.tile(indata, (1, 2))` 复制到左右声道
- 立体声麦克风 → 立体声扬声器：直接透传

### 3.6 UI 设计

#### 技术选择

选用 Tkinter 而不是 PyQt/Electron 的原因：
1. **零额外依赖**：Python 自带，不需要安装几十 MB 的 GUI 库
2. **启动快**：原生组件，秒开
3. **足够用**：这个软件界面简单，滑块、按钮、下拉框 Tkinter 完全能搞定
4. **体积小**：打包后体积远小于 Electron/PyQt

#### 界面布局（从上到下）

1. **标题区**：产品名称 + 副标题
2. **输入设备**：下拉选择 + 设备信息（声道/采样率/API）
3. **路由指示**：箭头 + "实时路由"文字
4. **输出设备**：下拉选择 + 设备信息 + 醒目提示条（声音从哪出来）
5. **麦克风增益**：50%~500% 滑块
6. **输出音量**：0%~200% 滑块
7. **防啸叫控制**：4 级防护开关 + 参数调节
8. **实时电平表**：输入/输出音量可视化（Canvas 绘制）
9. **状态信息**：延迟、缓冲占用、欠载次数
10. **控制按钮**：开始/停止路由、刷新设备

#### 深色主题

- 背景：`#1e1e2e`（深蓝灰）
- 面板：`#2a2a3e`
- 强调色：`#4f8ff7`（蓝色）
- 警告色：`#f59e0b`（琥珀色，用于防啸叫区域）
- 成功色：`#22c55e`（绿色）
- 危险色：`#ef4444`（红色）

---

## 四、性能指标

### 4.1 延迟分析

| 延迟来源 | 典型值 | 说明 |
|---------|--------|------|
| 蓝牙空中传输 | 100~200ms | 蓝牙协议本身的延迟，无法避免 |
| 音频处理 | 10~30ms | 增益、噪声门、重采样、颤音调制等 |
| 缓冲延迟 | 10~50ms | 环形缓冲区 + PortAudio 缓冲 |
| 扬声器输出缓冲 | 10~30ms | WASAPI 播放缓冲 |
| **总延迟** | **约 150~300ms** | 主要来自蓝牙 |

### 4.2 CPU 占用

- **空闲状态**：< 1%（UI 刷新）
- **运行状态**：2~5%（音频处理 + FFT 分析）
- **内存占用**：~50MB

### 4.3 啸叫抑制效果

| 算法 | 抑制效果 | 音质影响 | 延迟 |
|------|---------|---------|------|
| 颤音调制 | ⭐⭐⭐⭐ 强 | 极小（几乎听不出） | +2~5ms |
| 自适应陷波 | ⭐⭐⭐⭐ 强 | 极小（只影响啸叫频点） | ~1ms |
| 噪声门 | ⭐⭐⭐ 中（只在安静时有效） | 无（不说话时才静音） | 0 |
| 侧链抑制 | ⭐⭐ 辅助 | 小（动态压低） | 0 |

四级组合使用，基本可以完全消除啸叫。

---

## 五、已知限制与未来改进

### 5.1 已知限制

1. **蓝牙延迟**：蓝牙耳机麦克风的 100~200ms 延迟是蓝牙协议决定的，无法通过软件消除
2. **HFP 模式音质**：使用麦克风时，蓝牙耳机自动切换到 HFP（免提）模式，采样率只有 16kHz，音质为通话级别
3. **单设备限制**：目前只支持一对一的路由（一个输入 → 一个输出）
4. **Windows 限定**：界面和设备命名逻辑针对 Windows 设计

### 5.2 未来可扩展方向

| 方向 | 说明 |
|------|------|
| 多输出路由 | 同时输出到多个设备（扬声器 + HDMI + 蓝牙耳机） |
| AI 降噪 | 集成 RNNoise 等 AI 降噪算法 |
| 均衡器 | 10 段 EQ 调节音色 |
| 混响/效果 | 添加混响、延迟等音效 |
| 系统托盘 | 最小化到托盘，快捷操作 |
| 快捷键 | 全局快捷键控制开关、音量 |
| 配置保存 | 保存/加载多组设备预设 |
| 打包发布 | PyInstaller 打包成 exe，免安装 |
| macOS/Linux 支持 | 跨平台适配 |
| 虚拟音频设备 | 结合 VB-Cable 实现更多路由方式 |

---

## 六、快速使用指南

### 6.1 启动

双击 `start.bat` 启动程序。

### 6.2 使用步骤

1. 确保蓝牙耳机已连接电脑并设为可用
2. 程序会自动选择蓝牙麦克风 + 扬声器
3. 确认绿色提示条显示正确的输出设备
4. 点击「开始路由」按钮
5. 对着蓝牙耳机说话，声音从扬声器输出

### 6.3 啸叫调节建议

**默认参数已针对「不损伤人声」调优，大多数情况直接用即可。**

先说一条最重要的：**啸叫的根因是物理摆位，不是软件。** 麦克风和外放音箱处于同一个声场里，
声音就会绕回来形成正反馈。软件只能降增益、打散相位，消除不了物理环路。所以：

- 麦克风离音箱**越远越好（建议 ≥1.5 m，最好 ≥3 m）**
- 关掉 Windows「声音设置 → 录制 → 麦克风属性 → 侦听 → 侦听此设备」
- 能戴耳机就别外放——这是唯一彻底的办法

软件侧的调节顺序：

| 现象 | 先看这里 | 怎么调 |
|------|---------|--------|
| 还有啸叫 | ① 麦克风增益 | 降到 80~100%。**这是最有效的杠杆**，别用增益去换音量，音量大请调扬声器音量 |
| 还有啸叫 | ② 物理距离 | 挪远麦克风（收益远大于任何软件参数） |
| 还有啸叫 | ③ 陷波衰减 | 小步提高（10 → 12 → 15 dB）。**别超过 20 dB**，否则一旦误判对人声破坏不可逆 |
| 还有轻微啸叫 | ④ 移频 | 打开「频谱移频」，移频量 3~5 Hz。代价是整体音高固定偏高几 Hz |
| 觉得声音闷/变了 | 陷波面板的「当前陷波」显示 | **正常说话时应恒为「(无)」**。如果说话时就出现频率，说明在削你的共振峰 → 降低衰减强度、或关闭陷波 |
| 说话断断续续 | 噪声门 | 确认它处于**关闭**状态（默认已关）。它按电平判定，会吞掉字间停顿 |
| 音量忽大忽小 | 侧链抑制 | 确认它处于**关闭**状态（默认已关）。它会造成随语音起伏的自动增益 |
| 声音小 | 扬声器音量 | 调「扬声器音量」而不是麦克风增益 |

> **一句话**：默认值就是推荐值。遇到问题优先物理层，其次降增益，
> 而不是无脑加大陷波衰减和移频量——那正是初版「声音变了」的原因。

---

## 七、文件清单

| 文件 | 行数 | 说明 |
|------|------|------|
| `main.py` | 35 | 程序入口，异常捕获 |
| `start.bat` | 10 | 启动脚本（英文，避免编码问题） |
| `audio_router/models/audio_device.py` | 75 | 设备信息数据模型 |
| `audio_router/services/device_manager.py` | 247 | 设备枚举、识别、可用性检测 |
| `audio_router/services/audio_router.py` | 775 | 核心路由引擎、缓冲、增益、噪声门、侧链 |
| `audio_router/services/howling_suppressor_v2.py` | 527 | **真移频（FIR Hilbert）+ 多判据陷波**（当前使用） |
| `audio_router/services/feedback_suppressor_deprecated.py` | 420 | 【已废弃】旧颤音调制 + 单判据陷波，仅作历史记录 |
| `audio_router/ui/main_window.py` | ~1,100 | Tkinter 主界面 |
| `diagnostics/*.py` | ~1,000 | 诊断与回归验证脚本（不影响主程序） |
| **合计** | **~3,900** | |

---

*文档生成时间：2026-09-18*
*最后更新：2026-09-18 —— 第 3.3 节按「多判据陷波 + 真移频」重写，第 6.3 节调节建议改为以物理层优先；
初版诊断过程与实测数据见《啸叫问题诊断与解决方案报告.md》，回归验证见 `diagnostics/README.md`。*
